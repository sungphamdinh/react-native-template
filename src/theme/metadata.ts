export interface Theme {
  FONT: {
    regular: string;
    italic: string;
    bold: string;
  };
  FONT_SIZE: {
    small: number;
    medium: number;
    large: number;
  };
  COLORS: {
    background: string;
  };
}
